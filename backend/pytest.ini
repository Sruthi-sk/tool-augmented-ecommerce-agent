[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    integration: requires real API keys and network access (deselected by default)
    live: hits real PartSelect URLs via Crawl4AI/Playwright (deselected by default)
